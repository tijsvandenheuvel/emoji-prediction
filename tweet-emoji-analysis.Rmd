---
title: "Tweet analysis"
author: "Tijs Van den Heuvel"
date: "10/15/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(reticulate)
```
# Tweet sentiment analysis and emoji prediction

[data](https://www.kaggle.com/rexhaif/emojifydata-en)

I preprocessed this data to turn it into a dataframe in the jupyter notebook: 

_tweet-data-preproc.ipynb_

> the process seems to work for the small exerpt but not for the whole dataset

```{r}
tweets_red_df <- read.csv("~/bin/tweets/outv2/part-00000-759d612e-880f-4b68-9680-04e42ee361b7-c000.csv")

library(NLP)
library(tm)
corp= VCorpus(VectorSource(tweets_red_df$text))
```

```{r}
tweets_red_df$text[1]
tweets_red_df$emoji[1]
```

```{r}
writeLines(as.character(corp[[1]]))
```
## data cleaning
- how necessary is this, some things could be useful data

```{r}
corp <- tm_map(corp, removePunctuation)
#corp <- tm_map(corp, removeNumbers)
corp <- tm_map(corp,content_transformer(tolower))
```

```{r}
writeLines(as.character(corp[[1]]))
```


